apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fluorite.fullname" . }}-test-connection"
  labels:
    {{- include "fluorite.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fluorite.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
